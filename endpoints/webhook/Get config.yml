info:
  name: Get config
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/config/:identifier"
  params:
    - name: identifier
      value: "{{identifier}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
