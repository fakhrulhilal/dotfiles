info:
  name: Create docker hub config
  type: http
  seq: 7

http:
  method: POST
  url: "{{baseUrl}}/config"
  body:
    type: json
    data: |-
      {
        "identifier": "dockerhubpersonal4",
        "validate": false,
        "signature": null,
        "secret": null
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
