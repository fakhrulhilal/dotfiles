info:
  name: Get logs
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/webhook/:identifier"
  params:
    - name: identifier
      value: "{{identifier}}"
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
