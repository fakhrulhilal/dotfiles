info:
  name: Healthcheck
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/_health/ready"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
