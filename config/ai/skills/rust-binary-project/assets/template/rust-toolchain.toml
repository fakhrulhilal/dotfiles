# Nightly is needed to rebuild std for size (-Zbuild-std, panic = "immediate-abort");
# pinned to a date so builds are reproducible. Bump deliberately.
[toolchain]
channel = "__NIGHTLY__"
profile = "minimal"
components = ["rust-src", "rustfmt", "clippy"]
